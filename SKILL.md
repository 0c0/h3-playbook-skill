---
name: h3-playbook
description: MiniMax H3 视频生成的单一入口 skill（官方《使用手册》口径 + 官方 h3-prompt-writing 写作契约合并版）—— 三段式提示词公式、五类生成模式（T2VA / I2VA / FL2VA / L2VA / Ref2VA）的模式判定与写法差异、Ref2VA 六段改写格式、参考素材用途标签、能力边界（时长 / 分辨率 / 宽高比 / 输入上限 / 语言）、踩坑表。Use when 写或改写 H3 提示词、判断某个需求 H3 能不能做、选宽高比或时长、指定参考素材用途、需要 T2VA/I2VA/FL2VA/L2VA 的字段级结构或 Ref2VA 六段格式、排查口型不同步或切镜错乱。
skill_version: 2.0.0
---

# H3 Playbook

本 skill 由两份官方材料**合并**而成，是 H3 提示词写作的唯一入口：

1. MiniMax 官方《MiniMax H3 模型 - 使用手册》——能力边界、三段式公式、素材标签、踩坑表。
2. MiniMax 官方 skill `h3-prompt-writing`（`MiniMax-AI/MiniMax-H3` 仓库 `skills/h3-prompt-writing`，
   含 2026-08-11「Tips for Better Results」更新）——T2VA/I2VA/FL2VA/L2VA 的字段级最终结构
   与 Ref2VA 六段改写格式（原文在 `references/base-en.txt` / `references/ref-en.txt`，
   逐字节拷贝自官方，未改写）。

**功能边界**：只讲 H3 本身——提示词结构、生成模式、素材用途、能力边界、踩坑。
风格与语言**不由本 skill 预设**（不默认真人实拍、不默认禁止 3D/CG、不默认输出语言），按本次任务的实际要求走。
**在 ComfyUI 上怎么落地**（选哪支模型、参数怎么传、任务与产物怎么排障）不在本 skill，见配套的 `roundabout`；
任何机器相关的配置、硬件、实测数字都不写进来。

## 何时使用

- 写 / 改 / 扩写 H3 视频提示词（任何一种生成模式）。
- 判断「这个需求 H3 能不能做」、选时长 / 分辨率 / 宽高比、算输入素材够不够。
- 给上传的图 / 视频 / 音频标用途，或设计多素材分工。
- 出片有乱码 / 口型不同步 / 切镜错乱 / 莫名其妙多出 BGM，要找原因。

**不适用**：Qwen-Image-2.1 出图改图（`qwen-image-prompt-writing`）、Z-Image（`zimage-prompt-writing`）、
网关调用 / 注册 / 排障本身（`roundabout`）。

## 读取路由

| 什么时候 | 读哪个文件 |
|---|---|
| 判断「这需求 H3 能不能做」、选分辨率/宽高比/时长、算输入素材够不够 | `references/capability-limits.md` |
| 要给上传的图/视频/音频标用途（"这是人物参考还是风格参考"） | `references/ref-material-roles.md` |
| 需要一个完整三段式提示词的骨架照抄（含三种模式的骨架差异） | `references/skeletons.md` |
| 出片有乱码/口型不同步/切镜错乱/多出 BGM，或想看官方踩坑表与文字乱码对策 | `references/pitfalls.md` |
| 写 T2VA / I2VA / FL2VA / L2VA，需要**字段级**最终结构（对齐指令原文、三核心字段、时序记法） | `references/base-en.txt` |
| 写 Ref2VA（全参考改写），需要六段格式的完整规范（标签规则、保留分析、完整示例） | `references/ref-en.txt` |

## 第一步：判模式（每次必过的开关）

| 判据 | 模式 | 输出形态 | 结构读哪里 |
|---|---|---|---|
| 没有任何输入素材 | **T2VA** | 从文字搭全片视听时间线 | `base-en.txt` |
| 1 张图 = 首帧 | **I2VA** | 从首帧向后发展 | `base-en.txt` |
| 1 首帧 + 1 尾帧 | **FL2VA** | 描述两帧之间的连续路径 | `base-en.txt` |
| 只有尾帧 | **L2VA** | 从合理前情收敛到尾帧 | `base-en.txt` |
| 多素材参考 / 要编辑参考视频 | **Ref2VA** | 六段改写格式 | `ref-en.txt` |

模式要点：

- **T2VA**：描述要**更具体**（主体外观、场景细节、动作全写清），并用「大全景交代空间 + 中景承载动作 + 特写强调细节」的分层写法补信息量。
- **I2VA / FL2VA / L2VA**：**1 张图必须说清是首帧还是尾帧**（不说清模型会随机理解）；
  **2 张（首+尾）时 H3 不会自动加切镜**，只补两帧之间的动作/光影/声音——要切镜就别用这个模式。
- **Ref2VA**：每个素材逐一标用途（见「参考素材说明」），编辑类需求（换背景/删人物/替台词/迁移音色）都走这。

## 核心公式（每次都过）

```
完整提示词 = 参考素材说明 + 核心创意 + 画面过程说明
```

三段缺一不可，**没上传素材时第一段整段跳过**（不要写"无参考素材"这种占位，直接不写）。

**参考素材说明** —— 每个素材都要给「编号 + 用途」，编号按上传顺序。想保住的特征要**显式写出来**
（`@图片1 是人物参考，锁定：白金长发、窄黑复古墨镜、黑色高光漆皮风衣`），"传了图"不等于"模型知道你要保什么"。

**核心创意** —— 一句话锁定全片，必须含 5 项：主体（谁/什么）、地点（在哪）、事件（在做什么）、
题材/风格（写实/动画/电影感/广告片/纪录片…）、特殊运镜（不写就默认会切镜）。
切镜风格要挑一个明说：普通 cut / 叠化 fade / 卡点切 / 快切。

**画面过程说明** —— 按 shot 时间戳分段，每段写全 6 项：
**景别 + 内容 + shot 内运镜 + 动作 + 台词 + 音效**。再单独写一段「不想要」。

完整骨架（含三种模式差异 + 官方完整示例）照 `references/skeletons.md` 抄改。

### 两套编号口径，别混用也别慌

| 口径 | 长相 | 用在哪 |
|---|---|---|
| 中文手册口径 | `@图片1` / `@视频2` / `@音频3` | 中文手写提示词、三段式骨架 |
| 英文契约口径 | `<Picture 1>` / `<Video 1>` / `<Audio 1>` | 按 `base-en.txt` / `ref-en.txt` 写英文结构化提示词时 |

两者是同一件事的两种写法：`@图片N` ↔ `<Picture N>`（编号都按上传顺序）。**一次生成里从头到尾只用一套**。
网关实测：英文契约口径的 `<Picture i>` 在 Ref2VA 上**有效**——用它指定首帧/尾帧并锁特征清单，
尾帧可以近乎复刻参考图构图；物体参考只锁「写出的特征清单」（折法视角/比例没写就自由发挥）。

## 硬规则（合并两份官方材料）

1. **台词长度必须和 shot 长度对齐。** 口型问题 90% 出在这里——别在 3 秒的 shot 里塞一大段话。
   跨 shot 的台词 H3 支持 J-cut / L-cut，但要**明确写出**这句台词跨了哪几个 shot。
2. **要出现在画面里的文字/Logo/标题/按钮文案，必须把原文写出来。** 只说"加个标题"必翻车。
3. **少写比喻，多写看得见的画面。** H3 吃明确画面指令，不吃需要"意会"的抽象句。
   负面：「氛围要像一首忧伤的诗」；正面：「冷白侧前方柔光照亮人物面部，背景逆光，浅景深，竹林虚化成柔和散景」。

### Tips for Better Results（官方 2026-08-11 增补）

- **时长必须对齐**：描述覆盖的总时长 = 请求的视频长度（H3 区间 4–15 秒）。
- **参考标签全文一致**：`<Picture 1>` / `<Video 1>` / `<Audio 1>`（或 `@图片1`）在每个段落里都用同一个写法。
- **写具体视听细节，不写抽象词**：`cinematic`、`beautiful` 这类词模型替你猜的部分太多。
- **关键帧模式要点明衔接**：I2VA / FL2VA / L2VA 必须明确写出首帧和/或尾帧**怎样接入时间线**（0 秒处全参考、何时抵达尾帧）。

## Ref2VA 六段改写格式（编辑 / 全参考模式）

Ref2VA 的改写输出按固定顺序六段（标签全文一致，字段名逐字保留）：

```
subject_definitions → summary → retention_analysis → detailed_description
→ overall_soundscape → non_diegetic_music
```

- `detailed_description` 要**尽可能详细显式**：每个 shot 写清当前构图、主体外观与位置、环境与光、
  动作与状态变化、运镜、当前声音、参考内容实际出现/生效的时间点——**不要缩成剧情梗概**。
- 六段都用英文写；只有 `<d>` 里的台词/歌词与画面可见文字保留原语言。
- 标签规则、保留分析写法、完整示例：`references/ref-en.txt`。

## 输出前自检

- [ ] 模式判对（无素材=T2VA；有帧图按表；编辑/多素材=Ref2VA）
- [ ] 三段齐全（无素材时第一段跳过）；走英文契约时字段名与段落顺序照 `base-en.txt`/`ref-en.txt`
- [ ] 每个素材都标了编号 + 用途，特征清单显式写出
- [ ] 核心创意五要素齐全，切镜风格明说了
- [ ] 每个 shot 都有景别 + 内容 + 运镜 + 动作 + 台词 + 音效
- [ ] 台词长度 ≤ shot 长度；跨 shot 台词有写明；说话人归属（角色 / 画外音）清楚
- [ ] 画面文字写了原文
- [ ] 描述覆盖的总时长 = 请求时长，落在 4–15 秒
- [ ] 关键帧模式写明了帧怎样接入时间线
- [ ] 标签口径全文统一（`@图片N` 或 `<Picture N>`，不混用）
- [ ] 不要 BGM 时结尾写了 `非叙事性音乐：N/A` 或 `"non_diegetic_music": N/A`
- [ ] 写了「不想要」

## 官方来源与同步

- 中文口径：官方《MiniMax H3 模型 - 使用手册》（本 skill 最初版本的全部来源）。
- 英文契约：官方 `h3-prompt-writing` skill，本地拷贝对应其 **2026-08-11 版**（SKILL.md 加 Tips 的
  那次更新，commit `a107547`）；`base-en.txt` / `ref-en.txt` 与官方逐字节一致（仅换行符差异）。
- 官方再更新时：diff 官方仓库对应文件后同步进本 skill，并升 `skill_version`。
