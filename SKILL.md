---
name: h3-playbook
description: MiniMax H3 使用口径 —— 提示词三段式公式、三类生成模式（文生 / 图生首尾帧 / 全能参考）的写法差异、分镜与台词对齐规则、参考素材用途标签、能力边界（时长 / 分辨率 / 宽高比 / 输入上限 / 语言）。Use when 写或改写 H3 提示词、判断某个需求 H3 能不能做、选宽高比或时长、指定参考素材用途、排查口型不同步或切镜错乱。
---

# H3 Playbook

来源：MiniMax 官方《MiniMax H3 模型 - 使用手册》。本 skill 管**官方口径**（怎么写、能做什么）。

**功能边界**：只讲 H3 本身 —— 提示词结构、生成模式、素材用途、能力边界。
风格与语言**不由本 skill 预设**，按本次任务的实际要求走。
**在 ComfyUI 上怎么落地**（选哪支模型、参数怎么传、任务与产物怎么排障）不在本 skill，
见配套的 `roundabout`；任何机器相关的配置、硬件、实测数字都不写进来。

## 读取路由

| 什么时候 | 读哪个文件 |
|---|---|
| 判断"这需求 H3 能不能做"、选分辨率/宽高比/时长、算输入素材够不够 | `references/capability-limits.md` |
| 要给上传的图/视频/音频标用途（"这是人物参考还是风格参考"） | `references/ref-material-roles.md` |
| 出片有乱码/口型不同步/切镜错乱/莫名其妙多出 BGM | `references/pitfalls.md` |
| 需要一个完整三段式提示词的骨架照抄 | `references/skeletons.md` |

## 核心公式（每次都过）

```
完整提示词 = 参考素材说明 + 核心创意 + 画面过程说明
```

三段缺一不可，**没上传素材时第一段整段跳过**（不要写"无参考素材"这种占位，直接不写）。

**参考素材说明** —— 每个素材都要给「编号 + 用途」，编号按上传顺序（@图片1 / @视频2 / @音频3）。
想保住的特征要**显式写出来**，"传了图"不等于"模型知道你要保什么"。

**核心创意** —— 一句话锁定全片，必须含 5 项：主体（谁/什么）、地点（在哪）、事件（在做什么）、
题材/风格（写实/动画/电影感/广告片/纪录片…）、特殊运镜（不写就默认会切镜）。
切镜风格要挑一个明说：普通 cut / 叠化 fade / 卡点切 / 快切。

**画面过程说明** —— 按 shot 时间戳分段，每段写全 6 项：
**景别 + 内容 + shot 内运镜 + 动作 + 台词 + 音效**。再单独写一段「不想要」。

## 三条硬规则

1. **台词长度必须和 shot 长度对齐。** 口型问题 90% 出在这里 —— 别在 3 秒的 shot 里塞一大段话。
   跨 shot 的台词 H3 支持 J-cut / L-cut，但要**明确写出**这句台词跨了哪几个 shot。
2. **要出现在画面里的文字/Logo/标题/按钮文案，必须把原文写出来。** 只说"加个标题"必翻车。
3. **少写比喻，多写看得见的画面。** H3 吃明确画面指令，不吃需要"意会"的抽象句。

## 三类模式，写法不一样

- **纯文生**：描述要**更具体**（主体外观、场景细节、动作全写清），
  并用「大全景交代空间 + 中景承载动作 + 特写强调细节」的分层写法补信息量。
- **图生（首/尾帧）**：**1 张图**必须说清是**首帧**还是**尾帧**；
  **2 张（首+尾）**时 H3 **不会自动加切镜**，只补两帧之间的动作/光影/声音 —— 要切镜就别用这个模式。
- **全能参考（多模态）**：每个素材逐一标用途，例：
  `@图片1 人物参考（锁脸）`、`@视频1 动作参考`、`@音频1 情绪/节奏参考`。

## 输出前自检

- [ ] 三段齐全（无素材时第一段跳过）
- [ ] 每个素材都标了编号 + 用途
- [ ] 核心创意五要素齐全，切镜风格明说了
- [ ] 每个 shot 都有景别 + 内容 + 运镜 + 动作 + 台词 + 音效
- [ ] 台词长度 ≤ shot 长度；跨 shot 台词有写明
- [ ] 画面文字写了原文
- [ ] 不要 BGM 时结尾写了 `非叙事性音乐：N/A` 或 `"non_diegetic_music": N/A`
- [ ] 写了「不想要」
- [ ] 时长落在 H3 支持区间（4–15 秒）
